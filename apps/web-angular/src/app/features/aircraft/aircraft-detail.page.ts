import{Component,computed,inject,signal}from'@angular/core';import{ActivatedRoute,RouterLink}from'@angular/router';import{AircraftApiService}from'../../core/services/aircraft-api.service';import{AircraftOperation}from'../../core/models/aircraft.model';import{SEEDED_FLIGHTS}from'../../core/services/flight-api.service';
@Component({imports:[RouterLink],templateUrl:'./aircraft-detail.page.html',styleUrl:'./aircraft-detail.page.scss'})export class AircraftDetailPage{private readonly api=inject(AircraftApiService);private readonly route=inject(ActivatedRoute);readonly registration=this.route.snapshot.paramMap.get('registration')??'';readonly aircraft=signal<AircraftOperation|null>(null);readonly assigned=computed(()=>SEEDED_FLIGHTS.filter(f=>f.aircraft.includes(this.registration)));readonly loading=signal(true);constructor(){this.api.getAircraft().subscribe(items=>{this.aircraft.set(items.find(a=>a.registration===this.registration)??null);this.loading.set(false)})}}
